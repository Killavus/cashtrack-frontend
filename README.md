CashTrack Frontend
=========================

## Installation:

Install `node`:

```
brew install node # OSX
sudo apt-get install nodejs # Debian-based
sudo pacman -S node
```

Install `mimosa`:

```
npm -g install mimosa # as root if Linux
```

Run `make`.
